---
title: Install Yarn Package Manager Globally via npm or Corepack
description: Install Yarn globally using npm install or Node.js Corepack. Two methods to set up Yarn package manager for JavaScript and Node.js development.
---

<Tabs defaultValue="npm" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="npm">NPM</TabsTrigger>
    <TabsTrigger value="corepack">Corepack</TabsTrigger>
  </TabsList>
  <TabsContent value="npm">
    ```bash
    npm install -g yarn
    ```
  </TabsContent>
  <TabsContent value="corepack">
    ```bash
    corepack prepare --all && corepack enable yarn
    ```
  </TabsContent>
</Tabs>
