# Navigating The Pterodactyl Panel Database
There are many actions that can be done through the Pterodactyl panel or API, but there are also situations where being able to manually edit the database could save a lot of time.

<Tabs items = {['activity_log_subjects', 'activity_logs', 'allocations', 'api_keys', 'api_logs', 'audit_logs', 'backups', 'databases', 'egg_mount', 'egg_variables', 'eggs', 'failed_jobs', 'jobs' 
