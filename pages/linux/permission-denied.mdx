# Fix "Permission denied, are you root?"

## Running sudo
Try running the command with `sudo` in front of it like `apt update` would be `sudo apt update`.

## Alternatively, switch to the root user
Try `sudo su` to switch to the administrator user but if that doesn't work, try `su` or `su root`